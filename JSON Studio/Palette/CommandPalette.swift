// Palette — Phase 4 (SH-14). Cmd-Shift-P command palette: glass surface via glassBackground(),
// fuzzy filtering, right-aligned shortcuts, keyboard-only. Every command it exposes already
// exists in the menu bar — the palette is an accelerator, not a second definition point.
