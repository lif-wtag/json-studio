// Compare — Phase 3e. Separate window over JSONKit.StructuralDiff (ADR-07): side-by-side
// panes, synchronised scrolling anchored on matched nodes, the four diff classes as
// colour+SF Symbol+label, change summary, and a UI-exposed ArrayMatching strategy
// (index vs identity-key).
