// Editor — Phase 3b (highest risk). NSViewRepresentable around NSTextView + TextKit 2
// (ADR-04): CodeEditorView, CodeTextView, LineNumberRuler (NSRulerView), SyntaxHighlighter
// (incremental, visible-range only, 16 ms budget), BracketMatcher (span-tree driven).
// No SwiftUI TextEditor, no third-party editor, no WKWebView.
