// App/Commands — Phase 3a. The full native menu bar defined as SwiftUI `Commands`:
// App / File / Edit / JSON / View / Tools / Window / Help. Every keyboard shortcut is
// declared here (SH-02/SH-03), not in ad-hoc key handlers. Tools stays deliberately sparse
// so JSONPath / Schema / Transform (Phase 5) land without a redesign. The command palette
// (Phase 4) references these — it never defines new behaviour.
