// Inspector — Phase 3d. Tabs: Tree and Info (Statistics is Phase 4 — leave room).
// TreeView with type glyph+colour+label (never colour alone), array indices, expand/collapse.
// Bidirectional editor<->tree selection without feedback loops (IN-05/IN-06). Info tab copies
// value/key/path. Stays opaque — no glass (ADR-08).
